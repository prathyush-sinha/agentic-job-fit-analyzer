# Agent orchestration
langgraph
langchain-core

# LLM + embeddings
openai                 # reachable via EMBEDDING_PROVIDER=openai (kept as fallback)
google-genai           # default provider: Gemini free tier (embeddings + chat)

# Structured outputs / config
pydantic
pydantic-settings
python-dotenv

# Vector store (Postgres + pgvector)
psycopg[binary]
pgvector

# Hybrid retrieval (Phase 2)
rank-bm25              # sparse BM25 retrieval
sentence-transformers  # cross-encoder reranker (pulls torch, CPU)

# Serving
fastapi
uvicorn[standard]

# Testing
pytest
httpx  # required by fastapi TestClient
